#include <iostream>
#include <cmath>
#include "vec3.h"

void print_ppm_image(char * file_name, int width, int heigh, int ** image) {
    return;
}

bool plane_intersect() {
    return false;
}

bool sphere_intersect() {
    return false;
}

int main() {
    vec3 origin = vec3(0.0, 0.0, 3.0);
    vec3 gaze = vec3::normalize(vec3(1.0, 1.0, -0.5));
    return 0;
}